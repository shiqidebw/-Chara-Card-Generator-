# 本地项目数据（非缓存，不纳入版本控制）
.workbuddy/

# 本地数据库（运行时生成，含聊天记录与人格卡，切勿提交）
data.db
*.db

# Python 缓存
__pycache__/
*.pyc
*.pyo

# 本地环境变量（含密钥等，只提交 .env.example）
.env
*.env

# 编辑/运行产生的临时文件
*.log
.DS_Store

# 导出 ZIP 的临时落盘目录（下载后自动清理）
exports/

# 运行日志（按天滚动，非源码）
logs/
